# frozen_string_literal: true

RSpec.describe Bitz do
  it "has a version number" do
    expect(Bitz::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
